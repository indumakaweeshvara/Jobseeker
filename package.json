{
    "name": "jobseeker-workspace",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "cd JobSeekerApp && npx expo start",
        "android": "cd JobSeekerApp && npx expo start --android",
        "ios": "cd JobSeekerApp && npx expo start --ios"
    }
}